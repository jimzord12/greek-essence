---
name: bootstrap-next
description: Resume exactly one valid Greek Essence bootstrap task. Use when the operator says to continue the bootstrap, do the next bootstrap task, or explicitly invokes `$bootstrap-next`.
---

# Bootstrap Next Task

Read `.scratch/bootstrap/BOOTSTRAP-AGENTS.md` and execute exactly one next valid bootstrap task.

Treat that file and the documents it names as the complete execution workflow. Reconcile live repository state before work, use the required fresh implementer and reviewer profiles, and keep every Blocking/High correction cycle inside this root session using the original implementer and same reviewer. Complete review, closure, Ralph handoff/knowledge updates, and the required Task-ID commit. Return only after that one task is committed, genuinely blocked, or cannot continue safely.